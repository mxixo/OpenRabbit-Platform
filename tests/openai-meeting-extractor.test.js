"use strict";
const assert=require('assert');const {createOpenAiMeetingExtractor}=require('../integrations/openai/meeting-extractor');
(async()=>{let request;const extractor=createOpenAiMeetingExtractor({apiKey:'test-key',fetchImpl:async(url,options)=>{request={url,options,body:JSON.parse(options.body)};return {ok:true,json:async()=>({output:[{content:[{type:'output_text',text:JSON.stringify({candidates:[{isMeeting:true,confidence:.92,evidence:['Can we meet Thursday at 2?'],event:{title:'Investor meeting',start:'2026-08-20T14:00:00-07:00',end:'2026-08-20T14:30:00-07:00',timeZone:'America/Phoenix',location:'',description:'',attendees:[]}}]})}]}]})};}});const result=await extractor({from:'a@example.com',subject:'Meet Thursday',body:'Can we meet Thursday at 2?'});assert.equal(request.url,'https://api.openai.com/v1/responses');assert.equal(request.body.text.format.type,'json_schema');assert.equal(request.body.text.format.strict,true);assert.equal(result.length,1);assert.equal(result[0].event.title,'Investor meeting');console.log('openai-meeting-extractor.test.js passed');})().catch(e=>{console.error(e);process.exit(1);});
