# IaC
Provider-agnostic infrastructure-as-code module boundaries.
## Modules
- `modules/network` for networking primitives and segmentation contracts.
- `modules/compute` for service runtime capacity and scaling contracts.
- `modules/secrets` for secret integration contracts and access policies.
