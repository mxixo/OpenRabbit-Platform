# Production Release Gate Status

Status date: 2026-09-21. This tracker is evidence-based and intentionally conservative: a gate is not marked closed merely because a design document or unit test exists.

Source of gates: `docs/PRODUCTION_COMMERCIAL_READINESS.md`.

| Release gate | Status | Evidence present | Evidence still required before closure |
| --- | --- | --- | --- |
| No cross-tenant reads/writes | OPEN / PARTIAL FOUNDATION | Workflow idempotency scope includes tenant + workflow + version + key, with tests proving identical keys do not replay across tenants. The policy Target Scope Guard also hard-blocks an explicit cross-tenant action and binds external effects to expected/observed organization + connection/resource identity. | End-to-end tenant isolation across persistence, connectors, caches, receipts, APIs, background jobs, and negative/adversarial tests against real adapters and deployed storage. |
| Secrets never projected to worker models | OPEN / EVIDENCE REQUIRED | Architecture requires target verification/policy/credential brokering to stay outside worker authority, and the capability registry requires tenant-scoped provider credentials. This is architectural evidence, not a complete secret-flow proof. | Secret inventory, projection tests, redaction/egress tests, connector credential boundary tests, and incident-safe logging verification. |
| External writes are idempotent and provider-verified | OPEN / PARTIAL FOUNDATION | Workflow engine has deterministic tenant-scoped idempotency identity, claim-before-side-effect behavior, completed-result replay, and a durable single-host filesystem claim/complete store. Capability registry v1.2.0 refuses any declared `write_external` capability that lacks idempotency, trusted target verification, telemetry, or required provider verification receipts. Google runtime now performs provider-authoritative verification instead of inferring authority from local token presence. | Production multi-host/database-or-Redis CAS semantics, lease/recovery rules, connector-specific keys, provider-side verification/reconciliation implemented in every real write adapter, and deployed concurrency/restart evidence. |
| Retry does not duplicate external effects | OPEN / PARTIAL FOUNDATION | Completed results replay without invoking handlers again; storage failure before claim fails closed; same-host concurrent filesystem claimants produce one acquired + one in-progress result; completed state survives a process-style restart. | Crash-after-provider-write recovery, timeout ambiguity handling, provider reconciliation, multi-host contention tests, lease expiry/recovery policy, and deployed failure-injection evidence. |
| Policy engine cannot be bypassed by worker/runtime | OPEN / PARTIAL FOUNDATION | PR #94 adds an executable fail-closed policy/Target Scope Guard. Regression tests block target mismatch, missing/untrusted verification, ambiguity, cross-tenant access, audit bypass, and permission expansion. External targets must be asserted by trusted control-plane verifiers, not the worker model. | Route every production external-write adapter through the independent policy/target verifier; prove the worker cannot modify/impersonate the verifier, credential broker, audit sink, or network/broker boundary; add adapter-level bypass/fault-injection tests. |
| Provenance answers are deterministic from receipts | OPEN / PARTIAL FOUNDATION | Action Receipt contract now captures policy decision, provenance, external-side-effect intent, target-verification evidence, provider receipt identity, model/runtime fields, timestamps, idempotency identity, and status. | Persistent/versioned receipt storage plus golden tests that reconstruct the user-visible provenance answer deterministically across restart/version changes and missing-provider-receipt failure paths. |
| Revoke/reconnect/account-mismatch flows pass | OPEN / PARTIAL FOUNDATION | Merged Google runtime tests now verify protected UserInfo state, provider-side OAuth revocation, fail-closed refresh/rejection behavior, and cleanup of sibling Gmail/Calendar local state after the shared Google grant is revoked. The authenticated Connections route renders backend/provider-authoritative state rather than treating a UI click as proof of authority. | One clean hosted production-like account must complete the non-simulated `google_provider_e2e_v1` sequence: sign-in → read-only connect → provider-confirmed read → explicit incremental write consent → governed write → provider-side revoke → verified denied action → read-only reconnect/recovery. Wrong-account and permission-reduction negative evidence must also be retained. |
| Backup/restore and runtime restart drills pass | OPEN / EVIDENCE REQUIRED | Workflow idempotency has local restart-persistence unit evidence, but this is not a production backup/restore drill. | Documented backup policy plus tested restore drill, state consistency check, runtime restart/recovery drill, and recovery objectives. |
| Production monitoring and incident path exist | OPEN / PARTIAL FOUNDATION | PR #97 adds executable receipt-stream metrics for policy coverage, target-verification coverage, target-match rate, provider verification/reconciliation coverage, risk-class distribution, failure/confirmation/auto-execute rates, and p50/p95 execution + verification latency. Integrity warnings fire when required coverage falls below 100%. | Persist real production receipts/metrics, wire every connector/runtime path into the stream, add structured error taxonomy, traces/log correlation, idempotency/reconciliation metrics, alerts, ownership/escalation, incident playbook, rollback procedure, and deployed evidence. |
| Public claims match actually validated capabilities | OPEN / CONTINUOUS GATE | Project documents distinguish foundations from production-ready behavior; this tracker explicitly distinguishes tested control-plane contracts from deployed end-to-end production safety. | Pre-release claims inventory mapped to passing acceptance evidence; block unsupported reliability, security, automation, and performance claims. |

## Current production boundary gates

1. **OpenRabbit account/data boundary — issue #90.** Production intentionally fails closed until an explicit Supabase Auth/account project is designated. The connected organization currently exposes the inactive candidate `djplmhglilcwqfotnjew` plus the separate WayGo project; no project should be restored or inherited implicitly.
2. **Google hosted certification — issue #86.** After the account boundary is designated/configured, run the clean-account `google_provider_e2e_v1` sequence and retain metadata-only certification evidence. Unit/contract tests and route availability do not substitute for this provider evidence.
3. **WayGo source recovery — issue #95.** WayGo remains a separate product boundary. Its verified Supabase backend must not be treated as OpenRabbit's Auth/account project, and no WayGo itinerary persistence or front-end reconstruction should be inferred before the authoritative Hostinger baseline is recovered.

## Gate-closing rule

A release gate can move to **CLOSED** only when all of the following are attached or linked: (1) implementation evidence, (2) automated or reproducible acceptance evidence, (3) failure-path evidence, and (4) operational evidence where the gate depends on deployed behavior. Documentation alone is not closure.

## Immediate reliability sequence

1. Designate/configure the production OpenRabbit account boundary, then complete the hosted Google provider lifecycle certification without weakening least-privilege/incremental consent.
2. Wire the independent policy/Target Scope Guard and receipt emission into every production external-write adapter; the machine-readable capability contract requires this, but registry compliance alone is not runtime proof.
3. Replace the single-host filesystem reference backend with a production multi-host atomic CAS/transaction implementation (database or Redis) while preserving the current claim/complete contract.
4. Define claim lease/expiry and recovery semantics, especially the crash window where a provider write may have succeeded but local completion was not recorded.
5. Implement connector-specific idempotency-key derivation plus provider verification/reconciliation, then persist the action receipts that drive the policy-coverage metrics.
6. Add multi-host contention, crash-after-provider-write, timeout ambiguity, restart/recovery, tenant-negative, policy-bypass, provider-reconciliation, wrong-account, and permission-reduction fault-injection tests.
7. Stand up production telemetry/alerts plus incident/rollback and backup/restore drills, then re-evaluate this matrix before making production/commercial reliability claims.
