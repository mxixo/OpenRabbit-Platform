"use strict";
const fs=require('fs');
const path=require('path');
function check(env=process.env){const rows=[];const add=(name,ok,detail)=>rows.push({name,ok,detail});add('OpenRabbit org',Boolean(env.OPENRABBIT_ORG_ID),'OPENRABBIT_ORG_ID');add('API token',Boolean(env.OPENRABBIT_API_TOKEN&&env.OPENRABBIT_API_TOKEN.length>=32),'OPENRABBIT_API_TOKEN >= 32 chars');add('Actor ID',Boolean(env.OPENRABBIT_ACTOR_ID),'OPENRABBIT_ACTOR_ID');add('Google client ID',Boolean(env.GOOGLE_OAUTH_CLIENT_ID),'GOOGLE_OAUTH_CLIENT_ID');add('Google client secret',Boolean(env.GOOGLE_OAUTH_CLIENT_SECRET),'GOOGLE_OAUTH_CLIENT_SECRET');const tokenFile=path.resolve(env.GOOGLE_OAUTH_TOKEN_FILE||'.openrabbit/google-oauth.json');const direct=Boolean(env.GOOGLE_OAUTH_REFRESH_TOKEN);add('Google refresh token',direct||fs.existsSync(tokenFile),direct?'direct env override':tokenFile);add('OpenAI API key',Boolean(env.OPENAI_API_KEY),'OPENAI_API_KEY');const interval=Number(env.OPENRABBIT_MAIL_SCAN_INTERVAL_MS||300000);add('Scan interval',Number.isFinite(interval)&&interval>=60000,`${interval} ms`);return rows;}
if(require.main===module){if(fs.existsSync('.env')&&typeof process.loadEnvFile==='function')process.loadEnvFile('.env');const rows=check();for(const r of rows)console.log(`${r.ok?'PASS':'FAIL'}  ${r.name} — ${r.detail}`);if(rows.some(r=>!r.ok))process.exitCode=1;}
module.exports={check};
